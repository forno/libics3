language: cpp
sudo: false
dist: trusty
group: beta
compiler:
  - g++
  - clang++
install:
  - make
  - mkdir cbuild
  - cd cbuild
  - cmake .. -DCMAKE_INSTALL_PREFIX=install
  - make
  - make install
  - cd ..
script:
  ./bin/test
